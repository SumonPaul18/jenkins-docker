# Run Jenkins Container with Recommended Plugins
Start the Jenkins container:
####
    git clone https://github.com/SumonPaul18/jenkins-docker.git
    cd jenkins-docker
    docker compose up -d
    docker compose ps
    docker ps

    docker compose down
